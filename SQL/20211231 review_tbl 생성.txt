create table REVIEW_TBL(
    rv_number   number          PRIMARY key,
    m_id        varchar2(50)    not null,
    rv_date     date            default SYSDATE,
    rv_content  varchar2(2000)  not null
    rv_status number not null default 0
);

alter table REVIEW_TBL add CONSTRAINT fk_member_tbl_REVIEW_TBL_m_id FOREIGN key(m_id) REFERENCES member_tbl(m_id);

alter table review_tbl add CONSTRAINT fk_product_tbl_review_tbl_p_number FOREIGN key(p_number) REFERENCES product_tbl(p_number);

create sequence REVIEW_TBL_SEQ
start with 1
increment by 1;

commit;

